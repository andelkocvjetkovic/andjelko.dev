import { Image } from "astro:assets";

export function MyInfo({ avatar, name, position, company_url, company }) {
  return (
    <div class="flex max-w-[65ch] mx-auto pb-6">
      <div class="flex gap-x-6">
        <Image
          class="w-20 h-20 border-black border-2 rounded-md object-center object-cover"
          width={80}
          height={80}
          src={avatar}
          alt={name}
        />
        <div>
          <h1 class="text-2xl text-balance">{name}</h1>
          <p class="text-sm text-balance">
            {position} at{" "}
            <a
              href={company_url}
              class="text-orange-500 underline"
              target="_blank"
              rel="noopener noreferrer"
            >
              {company}
            </a>
          </p>
        </div>
      </div>
    </div>
  );
}
