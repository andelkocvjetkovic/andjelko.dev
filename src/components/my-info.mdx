import Avatar from './avatar.mdx'

export function MyInfo({ avatar, name, position, company_url, company }) {
  return (
    <div class="flex mx-auto pb-4">
      <div class="flex gap-x-3">
        <Avatar />
        <div>
          <h1 class="text-2xl xl:text-4xl text-balance pb-1">{name}</h1>
          <p class="text-xs font-light xl:text-sm text-balance">
            {position} at{" "}
            <a
              href={company_url}
              class="text-primary underline"
              target="_blank"
              rel="noopener noreferrer"
            >
              {company}
            </a>
          </p>
        </div>
      </div>
    </div>
  );
}
